# Ubuntu 14.04 Trusty support
sudo: required
dist: trusty
language: c++
addons:
  apt:
    sources:
    # add PPAs with more up-to-date toolchains
    - ubuntu-toolchain-r-test
    packages:
    # install toolchains
    - gcc-5
    - g++-5
    - gfortran-5

# Conifguration matrix
matrix:
  include:
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-4.9
      env: COMPILER=g++-4.9
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
      env: COMPILER=g++-5

before_install:
  openssl aes-256-cbc -K $encrypted_df55ab7ee3a4_key -iv $encrypted_df55ab7ee3a4_iv -in github_omni-compiler-mch-deploy_key.enc -out github_omni-compiler-mch-deploy_key -d

script:
  - mkdir build
  - cd build
  - cmake .. && make
  - make clean-transformation transformation test
